<UserControl x:Class="CSVEditor.View.Controls.CsvFileGridViewerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CSVEditor.View.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel x:Name="TopContainer">
        <TextBlock Text="Csv File Grid View Control IS ALIVE!!!"/>
        <!--<DataGrid x:Name="CsvFileDataGrid" 
                  CanUserResizeColumns="True" 
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding LocalCsvFile.Lines}"
                  >
            --><!--<DataGridTemplateColumn>
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}"/>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>--><!--            
        </DataGrid>-->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Label Content="Test Text:" Grid.Column="0"/>
            <TextBlock Text="{Binding TestText}" Grid.Column="1"/>
        </Grid>
        <ListView x:Name="OutterListView">
            
        </ListView>
    </StackPanel>
</UserControl>
